name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  basic-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Placeholder for now: just prove CI runs ---
      - name: Echo status
        run: |
          echo "CI is running for Catalyst. Replace this with real checks as services are implemented."
